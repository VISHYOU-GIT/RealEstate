# 🏗️ Real Estate Platform - System Architecture

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT SIDE                              │
│                     (React + Vite + Redux)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Browser    │  │   Browser    │  │   Browser    │          │
│  │  (Desktop)   │  │   (Tablet)   │  │   (Mobile)   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                  │
│                            │                                      │
│                   ┌────────▼────────┐                           │
│                   │  React Router   │                           │
│                   │  (Page Routing) │                           │
│                   └────────┬────────┘                           │
│                            │                                      │
│         ┌──────────────────┼──────────────────┐                 │
│         │                  │                   │                 │
│    ┌────▼────┐      ┌─────▼─────┐      ┌─────▼─────┐          │
│    │  Pages  │      │Components │      │  Redux    │          │
│    │  (13)   │◄─────┤  (5)      │◄─────┤  Store    │          │
│    └─────────┘      └───────────┘      └─────┬─────┘          │
│                                               │                  │
│                     ┌─────────────────────────┴─────────┐       │
│                     │                                   │       │
│              ┌──────▼──────┐                  ┌─────────▼─────┐│
│              │   Axios     │                  │  Socket.IO    ││
│              │  (HTTP)     │                  │  Client       ││
│              └──────┬──────┘                  └─────────┬─────┘│
│                     │                                   │       │
└─────────────────────┼───────────────────────────────────┼───────┘
                      │                                   │
                   HTTP/HTTPS                         WebSocket
                      │                                   │
                      ▼                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                         SERVER SIDE                              │
│                    (Node.js + Express.js)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Express Server                         │  │
│  │                    (Port 5000)                            │  │
│  └──────────────┬───────────────────────────┬────────────────┘  │
│                 │                           │                    │
│          ┌──────▼──────┐            ┌──────▼──────┐            │
│          │  REST API   │            │ Socket.IO   │            │
│          │  Endpoints  │            │   Server    │            │
│          └──────┬──────┘            └──────┬──────┘            │
│                 │                           │                    │
│     ┌───────────┼───────────┐              │                    │
│     │           │           │              │                    │
│ ┌───▼───┐  ┌───▼───┐  ┌───▼───┐      ┌───▼────┐              │
│ │ Auth  │  │Prop.  │  │Admin  │      │ Chat   │              │
│ │Routes │  │Routes │  │Routes │      │Events  │              │
│ └───┬───┘  └───┬───┘  └───┬───┘      └───┬────┘              │
│     │          │          │              │                      │
│     └──────────┴──────────┴──────────────┘                     │
│                           │                                      │
│                  ┌────────▼────────┐                           │
│                  │   Middleware    │                           │
│                  │  - Auth (JWT)   │                           │
│                  │  - Error Handle │                           │
│                  │  - Rate Limit   │                           │
│                  │  - CORS         │                           │
│                  └────────┬────────┘                           │
│                           │                                      │
│         ┌─────────────────┼─────────────────┐                  │
│         │                 │                 │                  │
│    ┌────▼────┐     ┌─────▼─────┐    ┌─────▼─────┐           │
│    │  Auth   │     │ Property  │    │ Contract  │           │
│    │Control. │     │Controller │    │Controller │           │
│    └────┬────┘     └─────┬─────┘    └─────┬─────┘           │
│         │                │                 │                  │
│         └────────────────┴─────────────────┘                  │
│                          │                                      │
│                  ┌───────▼────────┐                           │
│                  │  Mongoose ODM  │                           │
│                  └───────┬────────┘                           │
│                          │                                      │
└──────────────────────────┼──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                              │
│                      MongoDB Atlas                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌──────────┐ │
│  │   users    │  │ properties │  │  contracts │  │  chats   │ │
│  │ collection │  │ collection │  │ collection │  │collection│ │
│  └────────────┘  └────────────┘  └────────────┘  └──────────┘ │
│                                                                   │
│  Indexes:                                                        │
│  - users.email (unique)                                         │
│  - properties.location.coordinates (2dsphere)                   │
│  - properties.owner                                             │
│  - contracts.owner, contracts.tenant                            │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. User Authentication Flow

```
┌─────────┐
│ Browser │
└────┬────┘
     │ 1. Enter credentials
     ▼
┌─────────────┐
│ Login Page  │
└────┬────────┘
     │ 2. dispatch(login())
     ▼
┌─────────────┐
│ Redux Store │
└────┬────────┘
     │ 3. POST /api/auth/login
     ▼
┌──────────────┐
│ Auth Routes  │
└────┬─────────┘
     │ 4. Validate credentials
     ▼
┌──────────────┐
│    User      │
│   Model      │
└────┬─────────┘
     │ 5. Generate JWT token
     ▼
┌──────────────┐
│   Response   │
│ {token,user} │
└────┬─────────┘
     │ 6. Store in localStorage
     ▼
┌─────────────┐
│  Redirect   │
│ /dashboard  │
└─────────────┘
```

### 2. Property Creation Flow

```
┌────────────────┐
│ Create Property│
│     Page       │
│ (4-Step Form)  │
└────┬───────────┘
     │ 1. Fill form (Step 1-4)
     ▼
┌────────────────┐
│   Validation   │
└────┬───────────┘
     │ 2. dispatch(createProperty())
     ▼
┌────────────────┐
│  Redux Store   │
└────┬───────────┘
     │ 3. POST /api/properties
     │    Authorization: Bearer <token>
     ▼
┌────────────────┐
│   Auth        │
│  Middleware   │
└────┬───────────┘
     │ 4. Verify JWT
     ▼
┌────────────────┐
│   Property    │
│  Controller   │
└────┬───────────┘
     │ 5. Create property
     ▼
┌────────────────┐
│   Property    │
│    Model      │
└────┬───────────┘
     │ 6. Save to MongoDB
     ▼
┌────────────────┐
│   Response    │
│  {property}   │
└────┬───────────┘
     │ 7. Navigate to /my-properties
     ▼
┌────────────────┐
│ My Properties  │
│     Page       │
└────────────────┘
```

### 3. Real-Time Chat Flow

```
┌─────────┐                           ┌─────────┐
│ User A  │                           │ User B  │
└────┬────┘                           └────┬────┘
     │                                     │
     │ 1. Connect Socket.IO                │
     ├─────────────────┐                   │
     │                 │                   │
     ▼                 ▼                   ▼
┌────────────────────────────────────────────┐
│          Socket.IO Server                  │
│          (Backend)                         │
└────┬───────────────────┬───────────────────┘
     │                   │
     │ 2. joinRoom(propertyId)
     │                   │
     │ 3. Emit 'typing'  │
     ├───────────────────►
     │                   │ 4. Broadcast to room
     │                   ├─────────────────────►
     │                   │
     │ 5. Send message   │
     ├───────────────────►
     │                   │ 6. Save to DB
     │                   ▼
     │            ┌──────────────┐
     │            │ Chat Model   │
     │            └──────┬───────┘
     │                   │
     │                   │ 7. Broadcast message
     │◄──────────────────┤─────────────────────►
     │                   │
     ▼                   ▼
┌──────────┐        ┌──────────┐
│ Display  │        │ Display  │
│ Message  │        │ Message  │
└──────────┘        └──────────┘
```

### 4. Contract Signing Flow

```
┌──────────────┐
│ Contract     │
│ Details Page │
└────┬─────────┘
     │ 1. Click "Sign Contract"
     ▼
┌──────────────┐
│ Confirmation │
│   Dialog     │
└────┬─────────┘
     │ 2. Confirm
     ▼
┌──────────────┐
│ dispatch(    │
│signContract())│
└────┬─────────┘
     │ 3. PUT /api/contracts/:id/sign
     ▼
┌──────────────┐
│ Contract     │
│ Controller   │
└────┬─────────┘
     │ 4. Update signature
     │    - signed: true
     │    - signedAt: Date.now()
     │    - ipAddress: req.ip
     ▼
┌──────────────┐
│ Contract     │
│   Model      │
└────┬─────────┘
     │ 5. Check both signatures
     │    If both signed:
     │    status = 'active'
     ▼
┌──────────────┐
│  Response    │
│  {contract}  │
└────┬─────────┘
     │ 6. Update UI
     ▼
┌──────────────┐
│ Show Success │
│  + Badge     │
└──────────────┘
```

---

## 🗂️ Component Hierarchy

```
App
├── Layout
│   ├── Navbar
│   │   ├── Logo
│   │   ├── Navigation Links
│   │   └── User Menu
│   │       ├── Dashboard
│   │       ├── Profile
│   │       └── Logout
│   │
│   ├── Routes
│   │   ├── Public Routes
│   │   │   ├── Home
│   │   │   ├── Login
│   │   │   ├── Register
│   │   │   └── Properties (Browse)
│   │   │
│   │   └── Protected Routes
│   │       ├── Dashboard
│   │       ├── Profile
│   │       ├── My Properties
│   │       ├── Saved Properties
│   │       ├── Create Property
│   │       ├── Edit Property
│   │       ├── Contracts
│   │       ├── Contract Details
│   │       ├── Chats
│   │       └── Admin Dashboard (Admin Only)
│   │
│   └── Footer
│       ├── Company Info
│       ├── Quick Links
│       └── Social Media
│
└── Global Components
    ├── Loader (Spinner)
    ├── Toast Notifications
    └── ProtectedRoute (Auth Guard)
```

---

## 📦 Redux State Structure

```javascript
{
  auth: {
    user: {
      _id: "user_id",
      name: "John Doe",
      email: "john@example.com",
      role: "owner",
      reputation: 100
    },
    token: "jwt_token_here",
    isLoading: false,
    error: null
  },
  
  property: {
    properties: [
      {
        _id: "property_id",
        title: "Modern Apartment",
        price: 250000,
        location: {...},
        images: [...],
        // ... more fields
      }
    ],
    property: {...},  // Single property for details
    filters: {
      propertyType: "",
      listingType: "",
      minPrice: "",
      maxPrice: "",
      bedrooms: "",
      city: ""
    },
    pagination: {
      page: 1,
      pages: 5,
      total: 45
    },
    isLoading: false,
    error: null
  },
  
  contract: {
    contracts: [...],
    contract: {...},
    isLoading: false,
    error: null
  },
  
  chat: {
    chats: [...],
    messages: [...],
    activeChat: "property_id",
    isLoading: false,
    error: null
  }
}
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Security Layers                           │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Layer 1: Network Security                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • HTTPS/TLS Encryption                              │   │
│  │ • CORS Configuration                                │   │
│  │ • Rate Limiting (100 req/15min)                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                    │
│                          ▼                                    │
│  Layer 2: Application Security                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • Helmet.js (HTTP Headers)                          │   │
│  │ • Input Validation                                  │   │
│  │ • MongoDB Sanitization                              │   │
│  │ • XSS Protection                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                    │
│                          ▼                                    │
│  Layer 3: Authentication & Authorization                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • JWT Token (Bearer)                                │   │
│  │ • Password Hashing (bcryptjs)                       │   │
│  │ • Role-Based Access Control                         │   │
│  │ • Protected Routes                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                    │
│                          ▼                                    │
│  Layer 4: Data Security                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • Database Access Control                           │   │
│  │ • Connection String Encryption                      │   │
│  │ • Environment Variables                             │   │
│  │ • IP Address Tracking                               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Production Setup                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Frontend (Static Site)                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Vercel / Netlify / Render                          │   │
│  │  • React Build (Static Files)                       │   │
│  │  • CDN Distribution                                 │   │
│  │  • SSL Certificate                                  │   │
│  │  • Custom Domain                                    │   │
│  └──────────────────┬──────────────────────────────────┘   │
│                     │                                        │
│                     │ HTTPS Requests                         │
│                     ▼                                        │
│  Backend (Web Service)                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Railway / Render / Heroku                          │   │
│  │  • Node.js Server                                   │   │
│  │  • Express.js API                                   │   │
│  │  • Socket.IO Server                                 │   │
│  │  • Environment Variables                            │   │
│  │  • Auto-Scaling                                     │   │
│  │  • Health Checks                                    │   │
│  └──────────────────┬──────────────────────────────────┘   │
│                     │                                        │
│                     │ MongoDB Protocol                       │
│                     ▼                                        │
│  Database (Cloud)                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  MongoDB Atlas                                      │   │
│  │  • Managed Database                                 │   │
│  │  • Automatic Backups                                │   │
│  │  • Replication                                      │   │
│  │  • Monitoring                                       │   │
│  │  • Security                                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Performance Optimization

### Backend Optimizations
- ✅ Database indexing for fast queries
- ✅ Rate limiting to prevent abuse
- ✅ Compression middleware
- ✅ Efficient MongoDB queries
- ✅ Connection pooling

### Frontend Optimizations
- ✅ Code splitting (React lazy loading)
- ✅ Redux for state management (avoid prop drilling)
- ✅ Memoization where needed
- ✅ Optimized images
- ✅ Tree shaking (Vite)
- ✅ Production build optimization

### Database Optimizations
- ✅ 2dsphere index for geolocation
- ✅ Unique indexes on email
- ✅ Compound indexes on frequently queried fields
- ✅ Lean queries where possible

---

## 🔄 Real-Time Communication Architecture

```
Client A                 Server                  Client B
   │                       │                       │
   │ 1. Connect            │                       │
   ├──────────────────────►│                       │
   │                       │                       │
   │                       │ 2. Connect            │
   │                       │◄──────────────────────┤
   │                       │                       │
   │ 3. joinRoom(prop123)  │                       │
   ├──────────────────────►│                       │
   │                       │                       │
   │                       │ 4. joinRoom(prop123)  │
   │                       │◄──────────────────────┤
   │                       │                       │
   │ 5. sendMessage        │                       │
   ├──────────────────────►│                       │
   │                       │                       │
   │                       │ 6. Save to DB         │
   │                       ├─────┐                 │
   │                       │     │                 │
   │                       │◄────┘                 │
   │                       │                       │
   │ 7. newMessage event   │                       │
   │◄──────────────────────┤                       │
   │                       │                       │
   │                       │ 8. newMessage event   │
   │                       ├──────────────────────►│
   │                       │                       │
   │                       │                       │
```

---

## 📚 Technology Stack Summary

```
┌─────────────────────────────────────────────────────────────┐
│                  Complete Tech Stack                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Frontend:                                                   │
│  • React 18.2.0 (UI Library)                                │
│  • Vite 4.5.0 (Build Tool)                                  │
│  • Redux Toolkit 1.9.7 (State Management)                   │
│  • React Router 6.16.0 (Routing)                            │
│  • Tailwind CSS 3.3.5 (Styling)                             │
│  • Socket.IO Client 4.6.2 (WebSocket)                       │
│  • Axios 1.5.1 (HTTP Client)                                │
│  • React Hot Toast 2.4.1 (Notifications)                    │
│  • React Icons 4.11.0 (Icons)                               │
│  • date-fns 2.30.0 (Date Formatting)                        │
│                                                               │
│  Backend:                                                    │
│  • Node.js 18+ (Runtime)                                    │
│  • Express.js 4.18.2 (Web Framework)                        │
│  • Socket.IO 4.6.2 (Real-time)                              │
│  • Mongoose 7.6.3 (MongoDB ODM)                             │
│  • JWT (jsonwebtoken 9.0.2)                                 │
│  • bcryptjs 2.4.3 (Password Hashing)                        │
│  • PDFKit 0.13.0 (PDF Generation)                           │
│  • Helmet 7.1.0 (Security)                                  │
│  • Rate Limit 7.1.1 (Rate Limiting)                         │
│  • CORS 2.8.5 (Cross-Origin)                                │
│                                                               │
│  Database:                                                   │
│  • MongoDB Atlas (Cloud Database)                           │
│  • GeoJSON (Location Data)                                  │
│  • 2dsphere Indexing (Geo Queries)                          │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

This architecture ensures:
- ✅ Scalability
- ✅ Security
- ✅ Performance
- ✅ Maintainability
- ✅ Real-time capabilities
- ✅ Production readiness
